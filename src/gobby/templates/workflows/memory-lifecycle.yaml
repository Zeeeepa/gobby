name: memory-lifecycle
description: Standard memory lifecycle hooks
version: 1.0
type: lifecycle
author: Gobby
settings:
  priority: 50 # Run before handoff (which might clear context?)

triggers:
  on_session_start:
    - action: memory_inject
      min_importance: 0.5

  on_session_end:
    - action: memory_extract
    - action: skills_learn
