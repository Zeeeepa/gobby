---
name: agent/skill-hint
description: Hint message when a skill matches the user's prompt via trigger keywords
version: "1.0"
required_variables: [skill_name]
---
Relevant skill available: `get_skill(name="{{ skill_name }}")` on `gobby-skills`
