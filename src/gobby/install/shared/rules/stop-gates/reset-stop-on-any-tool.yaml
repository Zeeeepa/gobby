tags: [stop-gates, enforcement, lifecycle]

rules:
  reset-stop-on-any-tool:
    description: "Reset stop counter on any successful tool use"
    event: after_tool
    enabled: false
    priority: 80
    effect:
      type: set_variable
      variable: stop_attempts
      value: 0
