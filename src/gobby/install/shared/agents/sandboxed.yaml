name: sandboxed
description: |
  Agent that runs in an OS-level sandbox.
  Uses the CLI's built-in sandbox for isolation.

mode: headless
provider: claude
isolation: null
base_branch: main
workflow: generic
timeout: 120.0
max_turns: 10

sandbox:
  enabled: true
  mode: permissive
  allow_network: true
