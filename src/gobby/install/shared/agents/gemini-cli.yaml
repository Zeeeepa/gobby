name: gemini-cli
description: |
  Basic Gemini agent for interactive terminal use.
  No isolation, no workflow — just a helpful assistant.
  Uses the Gemini CLI's default model.

role: You are a helpful assistant
mode: terminal
terminal: auto
provider: gemini
isolation: null
sandbox: null
timeout: 120.0
max_turns: 30
