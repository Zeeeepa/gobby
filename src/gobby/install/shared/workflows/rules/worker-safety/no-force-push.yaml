tags: [worker-safety, gobby]

rules:
  no-force-push:
    description: "Block force push - never allowed."
    event: before_tool
    enabled: false
    when: "variables.get('is_spawned_agent')"
    effect:
      type: block
      tools: [Bash]
      command_pattern: "git\\s+push\\s+.*(?:--force(?:-with-lease|-if-includes)?|-f)(?:\\s|$)"
      reason: "Force pushing is never allowed."
