description = "Search and retrieve memories"
prompt = """
Search memories using gobby-memory MCP tools.

**Query:** {{args}}

Use the `recall` tool on the `gobby-memory` server with:
- query: "{{args}}" (or empty for recent memories)
- limit: 10

Display matching memories with their IDs, types, and importance scores.
"""
