#!/bin/sh
# Gobby pre-commit hook
# JSONL export has been moved to pre-push to avoid contamination in agent worktrees.

# Skip all pre-commit checks for spawned agents
if [ -n "$GOBBY_AGENT_RUN_ID" ]; then
    exit 0
fi
